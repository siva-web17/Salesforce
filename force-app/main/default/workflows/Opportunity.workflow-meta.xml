<?xml version="1.0" encoding="UTF-8"?>
<Workflow xmlns="http://soap.sforce.com/2006/04/metadata">
    <fieldUpdates>
        <fullName>UpdateOpportunityCloseDate</fullName>
        <description>Close date will be 6 months ahead from creation date.</description>
        <field>CloseDate</field>
        <formula>DATE( 
YEAR(TODAY()) + IF(MONTH(TODAY()) > 2, 1, 0), 
MOD(MONTH(TODAY()) + 2, 12), 
DAY(TODAY()) 
)</formula>
        <name>UpdateOpportunityCloseDate</name>
        <notifyAssignee>false</notifyAssignee>
        <operation>Formula</operation>
        <protected>false</protected>
    </fieldUpdates>
    <rules>
        <fullName>CloseDateIncreaseBy2Months</fullName>
        <actions>
            <name>UpdateOpportunityCloseDate</name>
            <type>FieldUpdate</type>
        </actions>
        <active>true</active>
        <description>New opportunities created should have closed date after 6 months from creation date when the creation date is not specified</description>
        <formula>NOT(ISBLANK(CloseDate))</formula>
        <triggerType>onCreateOnly</triggerType>
    </rules>
</Workflow>
